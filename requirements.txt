# Python 3.11+ Compatible Dependencies (Streamlit Cloud optimized)
# Core Streamlit & Data Stack
streamlit>=1.28.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# ML & Model Loading (CRITICAL - XGBoost required for model inference)
scikit-learn>=1.3.0,<2.0.0
xgboost>=2.0.0,<3.0.0
joblib>=1.3.0,<2.0.0

# Visualization (PyArrow excluded to avoid Python 3.13 build issues)
plotly>=5.14.0,<6.0.0

# MLflow for experiment tracking
mlflow>=2.8.0,<3.0.0

# Utilities
python-dotenv>=1.0.0,<2.0.0
requests>=2.31.0,<3.0.0

# NOTE: Avoid pyarrow, protobuf conflicts on Streamlit Cloud with Python 3.13
# If you need arrow support, pin to Python 3.11 in runtime.txt
